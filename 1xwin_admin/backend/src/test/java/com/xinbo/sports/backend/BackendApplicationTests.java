package com.xinbo.sports.backend;

import lombok.extern.slf4j.Slf4j;
import org.junit.jupiter.api.Test;

//@SpringBootTest
@Slf4j
class BackendApplicationTests {

    //    @Test
    void contextLoads() {
    }


    @Test
    void testConstructor() {
        //FinancialManagementParameter.DepositListReqDto dto = null;
      /*  var originList = List.of(1);
        List<FinancialManagementParameter.DepositListReqDto> list = null;
        list = originList.stream().map(x ->
                new FinancialManagementParameter.DepositListReqDto(x, "ww")
        ).collect(Collectors.toList());
        List<FinancialManagementParameter.DepositListReqDto> newList = list;
        newList.forEach(x ->
                log.info("xxxx=" + x.getId() + x.getName())
        );*/
    }


}
